# Benchmark for alya-lang/tensor

import "../src/lib.alya"

let A = Tensor.ones([64, 64])
let B = Tensor.ones([64, 64])

bench "GEMM Matrix Multiplication (64x64)"
    let C = A.matmul(B)
    C.free()
end

A.free()
B.free()
